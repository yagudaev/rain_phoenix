<h1>Edit Line item</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.order_line_item_path(@conn, :update, @order_id, @line_item)) %>

<span><%= link "Back", to: Routes.order_path(@conn, :show, @order_id) %></span>
